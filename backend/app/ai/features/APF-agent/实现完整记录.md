所有修改已完成。以下是对 deepseek_agent_with_tools.py 所做的变更汇总：

变更内容
1. 新增 SCRIPT_DIR 常量（第 10-11 行）

在 import os 之后立即计算脚本所在目录的绝对路径
所有后续的数据目录都基于这个路径
2. 移除全局单例的提前导入（原第 16-17 行）

删除了 from tools.memory_tools import memory_store 和 from skill_store import skill_store
这两行会在导入时立刻在 CWD 下创建 ./memories/ 和 ./skills/ 目录
AIAgent.__init__ 中保留了懒加载回退逻辑，不受影响
3. 修改 LOG_DIR（第 44 行）

从 LOG_DIR = "logs" 改为 LOG_DIR = os.path.join(SCRIPT_DIR, "logs")
4. 重写 main() 函数（第 1429-1452 行）

在 main() 中显式创建 MemoryStore、SkillStore、SessionStore，所有路径都指向 SCRIPT_DIR 下的子目录
最终目录结构
运行 deepseek_agent_with_tools.py 后，所有生成的数据都在脚本所在目录下：


APF-agent-02/
├── deepseek_agent_with_tools.py   ← 脚本本身
├── logs/                          ← API 调用日志
│   └── api_log_20260705_....log
├── memories/                      ← 持久记忆
│   ├── MEMORY.json
│   └── USER.json
├── data/                          ← 会话数据库
│   └── sessions.db
├── skills/                        ← 技能库
│   └── ...
└── .skill_tracking.json           ← 技能追踪数据
无论从哪个目录执行脚本，所有文件夹都会生成在脚本同级目录下。


---------------------------
---------------------------


所有修改已完成。以下是完整的变更汇总：

变更汇总
📁 新增文件
知识库模块（从 APF-agent-01 复制）：

knowledge/init.py — AFP 知识库入口
knowledge/motifs.py — 15 个 AFP 冰结合基序库（涵盖 5 大结构类别）
knowledge/literature.py — 10 条设计原则 + 12 个关键突变发现 + 8 种应用场景权重
knowledge/seq_analyzer.py — 三级序列分析管道（基序扫描→类型分类→全长评估）
模拟器模块（从 APF-agent-01 复制）：

simulator/init.py — 模拟器入口
simulator/geometry_scorer.py — 冰结合几何评分引擎（4 种冰面评分）
simulator/physicochemical_scorer.py — 物理化学特征评分
simulator/afp_predictor.py — AFP 概率预测（ICEPIC 等效）
simulator/simulator.py — 综合模拟集成接口
AFP 专用工具脚本（新建）：

工具脚本	工具名称	对应功能
tools/afp_knowledge_tools.py	afp_knowledge_query	知识库查询+多角度序列分析
afp_sequence_analyze	专注三级序列分析
tools/afp_mutation_tools.py	afp_mutate_sequence	精确序列突变（禁区检查）
tools/afp_evaluation_tools.py	afp_evaluate_mutation	突变多维度评估（PASS/WARNING/REJECTED）
tools/afp_simulation_tools.py	afp_ice_bind_simulate	冰结合虚拟实验模拟
tools/afp_strategy_tools.py	afp_design_strategy	设计策略推荐
afp_design_summary	设计结果汇总
✏️ 修改文件
main.py — 重写为 AFP 设计平台入口，支持交互模式和 --demo 模式
设计流程
智能体（LLM 驱动）会按以下流程自动完成抗冻蛋白设计：


用户输入序列 → afp_knowledge_query（分析）→ afp_design_strategy（策略）
    → afp_mutate_sequence（突变）→ afp_evaluate_mutation（评估）
    → afp_ice_bind_simulate（虚拟实验）→ 迭代优化
    → afp_design_summary（总结报告）
运行方式

cd APF-agent-02

# 交互模式
python3 main.py

# Demo 模式（自动启动 Type I HPLC6 抗冻蛋白设计）
python main.py --demo
运行效果：工具全部自动发现并加载（共 25 个工具，含 7 个 AFP 专用工具），Agent 在 LLM 驱动下自主调用工具完成完整的 AFP 设计流程，与 demo_output.txt 中的流程完全对应。




-----------------------
-----------------------

所有修复已完成。以下是对两个问题的变更汇总：

修复内容
问题 1: Agent 行为问题（重复调用工具、反问用户、突变前不检查序列）
修复位置: main.py — build_afp_system_prompt() 函数

具体改进:

禁止反问: 新增 ⚠️ 关键行为规则 节——"用户已给出序列和目标 → 直接进入设计流程，不要反问用户"，未指定场景时默认 cell_cryopreservation
禁止重复工具: "afp_knowledge_query 已包含完整分析，不要重复调用 afp_sequence_analyze"
突变前计数验证: 新增完整的序列位置对照示例（P1=D, P2=T, P3=A...），并要求"人工数一下位置确认 from_aa 正确"
关注相对变化: "只关注突变前后的变化趋势和变化百分比，而非绝对值"（因为简化模型估算偏低）
迭代要求: "至少 3 轮，不要一轮就放弃"
问题 2: LLM 计数错误导致突变失败（A16N 失败因为位置 16 是 N 不是 A）
修复位置: tools/afp_knowledge_tools.py — handle_knowledge_query()

具体改进: 新增 position_guide 字段，返回每 10 位一行的全序列位置对照表：


1:D 2:T 3:A 4:S 5:D 6:A 7:A 8:A 9:A 10:A
11:A 12:L 13:T 14:A 15:A 16:N 17:A 18:K 19:A 20:A
21:A 22:E 23:L 24:T 25:A 26:A 27:N 28:A 29:A 30:A
31:A 32:A 33:A 34:A 35:T 36:A 37:R
LLM 现在可以直接看到 P16=N，不会再尝试 A16N。

验证结果

1. knowledge_query → type=Type I, IBS=[2,13,24,35], P16=N ✅
2. strategy → 1 个 HIGH 优先级策略 ✅
3. mutate A3S,K18A → warnings=True（K18 是禁区盐桥残基）✅
4. evaluate → REJECTED（命中禁区）✅
5. simulate → TH/IRI 对比完成 ✅
6. summary → 1 轮汇总 ✅
K18A 自动被标记为禁区命中并 REJECTED——这正是正确的行为，系统现在能正确引导 LLM 避开禁区。


------------------------
------------------------

两个问题都已修复。变更汇总：

修复 1: 400 错误 (tool 消息没有对应的 tool_calls)
根因: AFP-agent.py 原来的 _truncate_history 直接保留最后 N 条消息，不检查 assistant(tool_calls) → tool 配对完整性，截断可能产生孤立的 tool 消息。

修改文件: AFP-agent.py

方法	变更
新增 _sanitize_history	全量扫描历史，删除所有前面没有 assistant(tool_calls) 的孤立 tool 消息
重写 _truncate_history	在 user 消息处安全截断，截断前后各调用一次 _sanitize_history
加固 _get_recent_exchange	返回前先 _sanitize_history，再去掉开头可能的孤 tool 消息
修复 2: 失败学习不生成 skill 文件
根因: _call_llm_for_reflection 的 system prompt 太弱——"你是Hermes的failure_learning助手。请根据对话历史做出决策，必要时调用工具"——LLM 默认选 add_memory 而非 skill_manage。

修改文件: AFP-agent.py + reflection.py

位置	变更
_call_llm_for_reflection	根据 context 参数定制 system prompt：skill_creation→"务必调用 skill_manage create"；failure_learning→"务必调用 skill_manage create 记录教训"
build_failure_reflection_prompt	从"使用 add_memory 或 skill_manage"改为**"你必须做：调用 skill_manage(action='create') 创建技能"**
build_reflection_prompt	从"请调用 skill_manage"改为**"你必须做：调用 skill_manage 创建技能"**
build_nudge_reflection_prompt	"优先创建技能而非记忆——技能是'如何做'，记忆是'是什么'"



-----------------
-----------------

所有修改已完成。以下是变更汇总：

变更总览
修改文件: AFP-agent.py
1. AIAgent.__init__ — 新增 max_design_rounds 参数（默认 30）

新增 max_design_rounds: int = 30 控制总设计轮次
新增 self.design_round_count = 0 追踪已完成轮次
2. chat() — 核心改造

位置	原值	新值
max_rounds	硬编码 5	动态计算: min(剩余设计轮×3+2, 60)
轮次解析	无	正则匹配 (\d+)\s*[轮论] 从用户输入提取轮次数
突变计数	无	每次 afp_mutate_sequence 调用时 design_round_count += 1
LLM 返回文本后	直接 return	检测是否需继续 → 自动注入继续指令 continue 循环
达到上限后	硬编码 "会话终止"	注入总结指令 → 再调用一次 LLM → 输出进展总结
提前结束	无	检测 LLM 回复中的完成标记（"设计完成"/"最终报告"等）→ 不再 continue
3. _sanitize_history / _truncate_history / _get_recent_exchange — 前次修复（防止 400 错误）

修改文件: main.py
System prompt 更新:

规则 5: "迭代至少3轮，默认最多30轮...系统会自动追踪设计轮次"
新增 ## 迭代提醒 节: 默认30轮、可用"运行N轮"调整、达到目标即结束、未达到继续
Agent 创建:


agent = AIAgent(max_design_rounds=30, ...)
使用方式

# 默认 30 轮
python main.py
序列: DTASDAAAAAALTAANAKAAAELTAANAAAAAAATAR
设计目标: 将TH活性提高到3倍或以上

# 语言指定轮次（如 50 轮）
> 运行50轮，设计抗冻蛋白序列 DTASDAAAAAALTAANAKAAAELTAANAAAAAAATAR

# Demo 模式（自动启动）
python main.py --demo
运行行为:

每完成一轮突变→评估→模拟 → 自动进入下一轮
达到设计目标 → LLM 调用 afp_design_summary → 提前结束
30 轮未达成 → 自动强制总结 + 反馈用户
轮次进度实时打印：🔄 自动继续设计 (第N/30轮)...


####################
全部完成。以下是变更汇总：

变更内容
1. 轮次标记（每轮开始/结束提示）
修改文件: AFP-agent.py — chat() 方法

效果: 终端输出从之前的大段无结构日志变为：


==================================================
🧬 Round 1/30 — 开始突变设计
==================================================
   🔧 afp_mutate_sequence: A3S, A17S ✅
   🔧 afp_evaluate_mutation: CAUTION ✅
   🔧 afp_ice_bind_simulate: TH=0.1 ✅
──────────────────────────────────────────────────
📊 Round 1 小结 | 判定: 🟡 CAUTION | TH +0.0% | IRI -3.5% | 剩余 29 轮
──────────────────────────────────────────────────

==================================================
🧬 Round 2/30 — 开始突变设计
==================================================
   ...
轮次开始: 每次调用 afp_mutate_sequence 时打印 🧬 Round N/30 — 开始突变设计
轮次结束: 当同一 LLM 响应中包含 afp_evaluate_mutation 时，打印小结（判定、TH/IRI 变化、剩余轮次）
纯分析阶段（afp_knowledge_query / afp_design_strategy）不触发轮次计数
进度一目了然：剩余 N 轮
2. 加载动画（Spinner）
新增代码: AFP-agent.py 第 12-44 行 — Spinner 类

效果: 每次调用 DeepSeek API 等待响应时，终端显示旋转动画：


   ⠋ LLM 推理中...
   ⠙ LLM 推理中...
   ⠹ LLM 推理中...
   ⠸ LLM 推理中...
后台线程运行，不阻塞主流程
API 响应返回后自动清除，不留痕迹
只在 LLM 调用期间显示，工具执行时不显示


################### 
全部完成。以下是变更汇总：

修改内容
1. 提前结束问题（10 轮就停止）
根因: chat() 的 auto-continue 只要检测到 "最终报告" 等关键词就直接停止，没有考虑最小轮次要求。

修复 (AFP-agent.py):

新增 min_design_rounds = 15 参数
修改 should_continue 逻辑：design_round_count < min_rounds 时无论如何都继续，即使 LLM 输出了"最终报告"
只有 design_round_count >= min_rounds 时才允许提前结束
continue prompt 改为：至少还需完成 N 轮设计。请继续调用 afp_mutate_sequence...
2. 默认轮次改为 15
位置	旧值	新值
AIAgent.__init__ 默认值	max_design_rounds=30	max_design_rounds=15
main.py agent 创建	max_design_rounds=30	max_design_rounds=15
System prompt	"默认最多30轮"	"默认至少运行15轮"
3. JSON 美化输出
修复 (AFP-agent.py _execute_tool_calls):


# 之前（紧凑难读）:
✅ 结果: {"success":true,"sequence":...}[200字符截断]

# 之后（缩进美观）:
✅ 结果:
{
  "success": true,
  "sequence": "DTASDAAAA...",
  "length": 37,
  ...
}
... (截断)
使用 json.dumps(indent=2) 美化输出
截断阈值从 200 增加到 600 字符
参数显示也做了截断保护（300 字符）
4. Skill 重试问题
根因: skill_manage 返回 "技能已存在" 被当作瞬时错误重试 2 次。

修复 (AFP-agent.py _execute_tool_calls):

新增非瞬态错误识别：已存在、不存在、权限、格式错误、不能为空
匹配到这些关键词 → 直接 break，不再重试


############################ 
全部完成。以下是变更汇总：

新增文件
design_recorder.py — 设计记录器
核心类 DesignRecorder，提供完整的会话数据管理：

功能	说明
session_id	格式 20260705_141754_579a9b73（时间戳 + UUID8）
set_baseline()	记录原始序列、设计目标、基线性能
record_round()	每轮记录突变、评估、模拟结果，实时写入 rounds.json
save_summary()	设计完成时生成 summary.json
generate_visualization()	生成 2×2 子图的 trajectory.png
tools/afp_visualize_tools.py — 可视化工具
新增 afp_visualize_trajectory 工具，LLM 可随时调用生成性能轨迹图。

目录结构
运行后自动生成：


APF-agent-02/
├── design_record/
│   └── 20260705_141754_579a9b73/     ← 唯一会话 ID
│       ├── rounds.json                ← 每轮设计数据
│       ├── summary.json               ← 最终汇总
│       └── trajectory.png             ← 性能变化轨迹图
rounds.json 格式

{
  "session_id": "20260705_141754_579a9b73",
  "original_sequence": "DTASDAAAAAALTAANAKAAAELTAANAAAAAAATAR",
  "design_target": "将TH提高到2.0°C",
  "total_rounds": 10,
  "rounds": [
    {
      "round": 1,
      "mutated_sequence": "DTSSDAAAA...",
      "mutations": ["A3S", "A17S"],
      "verdict": "PASS",
      "th_change_pct": 5.0,
      "iri_change_pct": -3.5,
      "th_value": 0.15,
      "iri_value": 1.56,
      "expression_score": 0.65,
      "stability_score": 0.61,
      "afp_probability": 0.85
    }
  ]
}
trajectory.png 可视化
4 个子图：

左上: TH 活性变化（红线，含基线虚线）
右上: IRI 活性变化（蓝线，含基线虚线）
左下: 表达（绿线）+ 稳定性（橙线）评分变化
右下: AFP 概率趋势（紫线，含 0.9 高置信线）
修改文件
AFP-agent.py:

导入 DesignRecorder，在 __init__ 中创建实例
afp_knowledge_query 首次调用时自动记录基线
每轮突变+评估+模拟后自动调用 record_round() 存盘
设计完成时自动 save_summary() + generate_visualization()
终端打印会话 ID 和存储路径

################## 

全部完成。最终目录结构如下：

会话目录结构

design_record/
└── 20260705_144647_7483bd37/       ← 唯一会话 ID（时间戳_UUID8）
    ├── rounds.json                  ← 每轮设计数据（实时更新）
    ├── summary.json                 ← 最终汇总
    ├── input_seq_analysis/
    │   └── analysis.json            ← 输入序列完整评估（afp_knowledge_query 结果）
    ├── images/
    │   └── trajectory.png           ← 性能变化轨迹图（4 子图）
    └── report/
        └── final_report.json        ← 最终设计报告
变更文件
文件	变更
design_recorder.py	新增子目录管理 + save_input_analysis() 方法
AFP-agent.py	用 save_input_analysis() 替换旧的 set_baseline()
tools/afp_visualize_tools.py	图表路径改为 images/trajectory.png
analysis.json 内容
保存 afp_knowledge_query 的完整结果，包含：

序列信息、AFP 类型预测、置信度
IBS 位置、靶向冰面
物理化学特征（Thr/Ala/Cys 含量、GRAVY、pI 等）
禁区列表、突变热点
活性预测基线（TH/IRI/表达/稳定性）
相关设计原则、文献突变发现


完整原始结果（full_result 字段）


################### 
全部完成。最终会话目录结构和变更如下：

最终目录结构

design_record/
└── 20260705_151054_e2d1e7a2/        ← 唯一会话 ID
    ├── rounds.json                    ← 每轮数据（实时更新）
    ├── summary.json                   ← 最终汇总
    ├── input_seq_analysis/
    │   └── analysis.json              ← 输入序列完整评估
    ├── images/
    │   └── trajectory.png             ← ✅ 性能变化轨迹图（4 子图）
    └── report/
        ├── final_report.json          ← JSON 格式报告
        └── final_report.md            ← ✅ Markdown 全面报告
修复内容
1. 图片缺失问题
根因: generate_visualization() 只在 chat() 的某个分支中单独调用，如果设计提前结束或走其他路径，就不会生成。

修复: 将 generate_visualization() 集成到 save_summary() 内部，只要调用 save_summary() 就自动生成图表，不再依赖外部调用。

2. Markdown 报告
新增 _generate_markdown_report() 方法，生成 7 章节的全面报告：

章节	内容
§1 输入序列分析	原始序列、基线 TH/IRI/表达/稳定性
§2 突变设计路径	每轮突变、判定图标（✅🟡❌）、TH/IRI 变化
§3 序列对比	原始 vs 最终序列，突变位点差异
§4 性能对比	基线 vs 最终（含变化百分比）
§5 性能变化趋势	逐轮 TH/IRI/表达/稳定性/AFP 概率数值表
§6 可视化	嵌入 trajectory.png 图片引用
§7 设计经验总结	成功策略 / 应避免突变 / 最佳轮次
变更文件
文件	变更
design_recorder.py	新增 _generate_markdown_report()；save_summary() 内集成可视化
AFP-agent.py	简化 —— 移除单独的 generate_visualization() 调用


########################## 
全部完成。变更汇总：

会话唯一性保障
新增机制
机制	说明
UUID 唯一 ID	时间戳_UUID8 格式（如 20260705_151744_d721295f），每次运行必然不同
碰撞保护	万一目录已存在，自动追加 _1, _2 计数器
sessions_index.json	design_record/ 根目录下的会话索引文件，记录所有历史会话
list_sessions()	静态方法，可随时查询所有历史会话
零删除	整个 design_recorder.py 无任何删除/覆盖逻辑
运行时提示
启动 main.py 时，终端会打印：


📁 会话 ID: 20260705_151744_d721295f
📁 数据目录: /Users/.../design_record/20260705_151744_d721295f
📁 历史会话数: 3
多次运行效果

design_record/
├── sessions_index.json              ← 所有会话索引
├── 20260705_151200_abc12345/        ← 第1次运行
│   ├── input_seq_analysis/
│   ├── images/
│   └── report/
├── 20260705_151500_def67890/        ← 第2次运行（不覆盖旧的）
│   ├── ...
└── 20260705_151744_d721295f/        ← 第3次运行（不覆盖旧的）
    └── ...
变更文件
文件	变更
design_recorder.py	碰撞保护 + _write_session_index() + list_sessions()
main.py	启动时打印会话 ID 和历史会话数


########## 
修复完成，目前 design_record/ 下只有一个会话文件夹，所有文件都在正确位置：


design_record/
└── 20260705_151248_e72aeb9c/       ← 唯一会话
    ├── summary.json
    ├── input_seq_analysis/
    │   └── analysis.json
    ├── images/
    │   └── trajectory.png           ✅ 图片在正确位置
    └── report/
        ├── final_report.json
        └── final_report.md
修复内容
根因: afp_visualize_trajectory 工具每次调用时自己创建新的 session_id，导致出现第二个只含图片的孤立会话文件夹。

修复 (tools/afp_visualize_tools.py):

不再自己生成 session_id
改为自动发现最新会话目录，将图片保存到已有会话的 images/ 下
如果没有任何会话（极端情况），保存到 design_record/visualizations/ 独立目录，不伪造会话 ID